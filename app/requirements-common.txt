# Azure Service Bus messaging
azure-servicebus>=7.14,<8.0.0

# Azure Web PubSub (server-side publish + token generation)
azure-messaging-webpubsubservice>=1.3.0,<2.0.0

# OpenTelemetry exporters (Azure Monitor + OTLP HTTP)
# Pinned for compatibility; versions must stay in sync across API and Worker.
# azure-monitor-opentelemetry-exporter==1.0.0b48 currently resolves against
# opentelemetry-api/sdk/exporters 1.39.0, not 1.39.1.
opentelemetry-api==1.40.0
opentelemetry-sdk==1.40.0
opentelemetry-exporter-otlp-proto-http==1.40.0
azure-monitor-opentelemetry-exporter==1.0.0b48

# Table storage for scan results
azure-data-tables>=12.7,<13.0.0

# Blob storage for screenshots
azure-storage-blob>=12.28,<13.0.0

# Local dev backends (queue/results)
redis>=7.1
