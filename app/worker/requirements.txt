# Azure Service Bus messaging
azure-servicebus>=7.14,<8.0.0

# Azure Web PubSub (server-side publish)
azure-messaging-webpubsubservice>=1.3.0,<2.0.0

# OpenTelemetry exporters (Azure Monitor + OTLP HTTP)
# Compatibility lock: azure-monitor-opentelemetry-exporter 1.0.0b45 depends on
# LogData in opentelemetry.sdk._logs, which is not present in 1.39+.
opentelemetry-api==1.38.0
opentelemetry-sdk==1.38.0
opentelemetry-exporter-otlp-proto-http==1.38.0
azure-monitor-opentelemetry-exporter==1.0.0b45

# Table storage for scan results
azure-data-tables>=12.7,<13.0.0

# Blob storage for screenshots
azure-storage-blob>=12.28,<13.0.0

# HTTP client for web fetching
requests>=2.32

# Local dev backends (queue/results)
redis>=7.1

# HTML parsing
beautifulsoup4>=4.14

# Ad/tracker detection
adblockparser>=0.7

# Domain extraction
tldextract>=5.3

# YARA pattern matching
yara-python>=4.5

# Browser automation for screenshots
playwright>=1.57
